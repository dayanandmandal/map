import { ResultValuePipe } from './result-value.pipe';

describe('ResultValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ResultValuePipe();
    expect(pipe).toBeTruthy();
  });
});
